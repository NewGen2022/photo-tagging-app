import Layout from '../components/layout/Layout';

const Leaderboard = () => {
    return (
        <Layout>
            <div>LEADERBOARD</div>
        </Layout>
    );
};

export default Leaderboard;
